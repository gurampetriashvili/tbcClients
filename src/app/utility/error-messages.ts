export enum ErrorMessages {
    required = 'field is required',
    minlength = 'Minimum amount of characters required ',
    maxlength = 'Maximum amount of allowed characters exceeded',
    pattern = 'Invalid format',
}
